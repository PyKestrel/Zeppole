apiVersion: v1
kind: Service
metadata:
  name: zeppole-web
  labels:
    app.kubernetes.io/name: zeppole
    app.kubernetes.io/component: web
spec:
  selector:
    app.kubernetes.io/name: zeppole
    app.kubernetes.io/component: web
  ports:
    - port: 80
      targetPort: 80

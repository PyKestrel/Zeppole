apiVersion: v1
kind: Service
metadata:
  name: zeppole-api
  labels:
    app.kubernetes.io/name: zeppole
    app.kubernetes.io/component: api
spec:
  selector:
    app.kubernetes.io/name: zeppole
    app.kubernetes.io/component: api
  ports:
    - port: 4000
      targetPort: 4000

# Zeppole vendor snapshot

This directory is a **one-time vendor snapshot** used to build the `zeppole-emulator` image. It is not an ongoing git submodule of the upstream project.

Telemetry, Google Forms, and related analytics have been **removed** from the Python device bootstrap. See the repository root `POLICY-NO-TELEMETRY.md`.

Legacy upstream analytics documentation in this tree has been **deleted** and replaced by this notice.

<!-- vendored in Zeppole; not a live upstream submodule -->
